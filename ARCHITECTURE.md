# アーキテクチャ

## システム境界

このフレームワークはimport済みのPython commandパッケージを受け取り、Click互換のルートコマンドを生成します。探索、ルート構築、シグネチャ変換、実行Context、ライフサイクル制御を担当します。トークン解析、標準ヘルプ表示、用法エラー、テスト実行はClickが担当します。

```text
commandパッケージ
      │
      ▼
ファイル探索 ──► ルート記述子 ──► Clickコマンドツリー
                      │                    │
                binding解決               ▼
                                  ライフサイクル実行
                                              │
                                              ▼
                                       利用者のcommand()
```

## コンポーネント

- **探索**：パッケージパスを走査し、補助モジュールをimportせずにPython構文からcommandモジュールを特定します。
- **ルートモデル**：Clickから独立して、グループ、末端コマンド、ソースモジュール、docstring、bindingプレースホルダーを表現します。
- **引数アダプター**：対応する関数シグネチャをClickの引数とOptionへ変換します。
- **Clickアダプター**：ルート記述子をネストした遅延読み込み対応のClickコマンドとグループとして公開します。
- **ランタイム**：現在のContextを設定し、setup、command、teardownを実行して、結果や失敗を終了動作へ変換します。
- **Context**：読み取り専用bindings、任意利用の可変state、teardown中の例外を公開します。

## 依存方向

探索とルートモデルはClickへ依存しません。引数変換では、Clickアダプターが利用する前にフレームワーク中立の引数記述を生成できます。ランタイムはルートモデルとContextへ依存でき、外側のアダプターだけがClickへ直接依存します。

利用者のcommandパッケージは公開`cli_framework` APIだけに依存し、内部モジュールをimportしてはいけません。

## 探索境界

呼び出し側は任意のファイルシステムパスではなく、import済みのパッケージを渡します。パッケージの`__path__`を探索起点、パッケージ名をimportパスとして使用します。これによりwheelへインストールした後も通常の相対importを維持できます。

探索処理は`ast`でソースを解析し、トップレベルの`command`、`setup`、`teardown`関数とモジュールdocstringを検出します。トップレベルの`command`を持たないモジュールは末端コマンドではありません。到達可能なコマンドを含むディレクトリはグループになります。

## 中核となる不変条件

- ファイルシステムの各区間をCLI名へそのまま対応させ、ファイル名のunderscoreを維持します。
- binding置換はファイルstemまたはディレクトリ区間全体だけに適用します。
- 解決後のルートツリーでは、同じ親を持つ名前が一意になります。
- command判定だけを目的として補助モジュールをimportしません。
- ヘルプ表示で利用者のライフサイクル処理を実行しません。
- 正常終了した各setupに対し、対応するteardownを最大1回実行します。
- teardownは正常終了した内側のスコープから外側へ実行します。
- 現在のContextを別の実行へ漏洩させません。
- 固定子コマンド名を、直接実行可能なグループの位置引数より優先します。

## 互換性

初版の公開契約はPython 3.10+と、互換性のある現行Clickを対象にします。配布ソースではPython 3.10が解析できない新しい構文を公開APIに使用しません。command作成者は`typing.Annotated`を使用でき、`X | None`形式もPython 3.10で利用できます。

## 将来の拡張

ルートと引数のモデルは、複数値bindings、追加のPython型、独自変換、非同期実行を後から追加できる構造にします。ただし、初版では将来を推測したAPIを公開しません。
