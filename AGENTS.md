# エージェントガイド

## 目的

このリポジトリでは、Pythonパッケージのディレクトリ構造から階層CLIを構築するフレームワークを開発します。

このファイルは簡潔な目次として保ち、製品仕様や詳細設計を転載しないでください。

## 最初に読む文書

現在のタスクに必要な文書だけを読みます。

1. `.review-work/notes/tasks.md`：ローカルのタスク境界、依存関係、検証コマンド（存在する場合）
2. `docs/product-spec.md`：利用者から見える振る舞いと受け入れ基準
3. `ARCHITECTURE.md`：コンポーネント、依存方向、不変条件
4. `docs/design.md`：詳細なインターフェースと境界条件
5. `docs/development.md`：環境の準備と検証方法

`.review-work/notes/`の履歴は経緯の調査時だけ読みます。この領域はGit対象外です。新しいチェックアウトでは、依頼に応じて作業一覧を作成します。

## 正式な情報源

- 利用者から見える振る舞い：`docs/product-spec.md`
- 公開Python APIとアルゴリズム：`docs/design.md`
- コンポーネント境界：`ARCHITECTURE.md`
- ローカルの作業状況：`.review-work/notes/tasks.md`（製品仕様の情報源にはしない）
- パッケージ設定：`pyproject.toml`、検証コマンド：`docs/development.md`

文書間に矛盾がある場合は、適切な正式情報源で整合させます。解釈を暗黙に選択してはいけません。

## スコープの制約

初版はPython 3.10+を対象とし、内部でClickを使用します。

タスクで明示されない限り、次の機能を追加しません。

- 独自の引数パーサー
- 非同期command関数
- DIコンテナ
- 設定ファイル管理
- ロギングフレームワーク
- Rich固有の表示
- プロジェクト生成コマンド
- `Path`、`Enum`、`Literal`、コレクション、独自の型変換

返却するClick互換ルートコマンドを除き、Clickの詳細を公開APIから隠します。

## 想定するパッケージ構成

```text
src/cli_framework/
    __init__.py
    context.py
    discovery.py
    errors.py
    model.py
    parameters.py
    runtime.py
tests/
    fixtures/
    test_*.py
```

タスクの実装でより明確な境界が判明した場合だけ構成を変更し、`ARCHITECTURE.md`も更新します。

## タスクの進め方

`.review-work/notes/tasks.md`に依頼のタスクを記録し、未完了タスクを一度に1つだけ扱います。

- 着手前に、依存タスクがすべて完了していることを確認します。
- 関連する仕様節を読み、変更をタスクの境界内に保ちます。
- タスクに対応する焦点を絞ったテストを追加または更新します。
- 完了済みタスクが確立した公開動作を維持します。
- 無関係な整形やクリーンアップを避けます。
- 可能な限りcommandモジュールのimport時処理を避けます。
- タスク記載の検証コマンドと、変更範囲に関係する全テストを実行します。
- 検証成功後にだけタスクを完了状態へ変更します。
- 設計と実際が異なった場合は、タスクの下に短い結果を記録します。

## コーディング規約

- 型が付き、入力と出力が明確な小さい関数を優先します。
- 内部のファイルシステム操作には`pathlib.Path`を使います。
- 製品コードは`sys.path`を変更せず、`importlib` APIを使います。テストでは一時パッケージの探索に`monkeypatch.syspath_prepend`を使用できます。
- 実行中Contextには`contextvars.ContextVar`を使います。
- 定義エラーとcommand実行失敗を区別します。
- 定義診断にはソースモジュール名と引数名を含めます。
- teardownも失敗した場合は、元のcommand例外を優先します。
- ヘルプ生成中にsetup、command、teardownを実行しません。
- 探索時に非commandの補助モジュールをimportしません。

## テスト規約

- 単体テストと結合テストにはpytestを使います。
- CLIの観測可能な動作にはClickの`CliRunner`を使います。
- テスト用commandパッケージの共通生成器・固定素材は`tests/fixtures/`以下に置きます。動的なケースは一時領域へ生成し、テスト後にimportキャッシュを復元します。
- stdout、stderr、終了コードを個別に検証します。
- エラー文は装飾ではなく、安定した意味を検証します。
- Windows互換のパスを使い、特定シェル専用の構文を避けます。
- テストを実行順や共有グローバル状態へ依存させません。

## 文書管理

- このファイルは約100行の目次として保ちます。
- 利用者向け要件は`docs/product-spec.md`へ記録します。
- アルゴリズムとAPI契約は`docs/design.md`へ記録します。
- 個人的な作業計画・進捗・履歴は`.review-work/notes/`へ記録し、コミットしません。
- 継続して必要な仕様・設計判断は正式な文書へ反映し、ローカル記録だけに残しません。
- このファイルを肥大化させず、必要なら焦点を絞った設計文書を追加します。
